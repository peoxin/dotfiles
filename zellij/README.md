# Zellij

Install `zellij`:

```
sudo pacman -S zellij # on Arch Linux
brew install zellij # on MacOS
```

Symlink the configuration files:

```
mkdir -p ~/.config/zellij
ln -s $DOTFILES/zellij/config.kdl ~/.config/zellij/config.kdl
```

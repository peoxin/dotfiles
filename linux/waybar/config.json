[
  {
    // Bar settings
    "layer": "top",
    "position": "top",
    "output": ["eDP-1"],

    "include": [
      "~/dotfiles/waybar/modules.json",
      "~/dotfiles/waybar/modules-limit-length.json"
    ],

    // Modules
    "modules-left": ["wlr/workspaces", "hyprland/submap", "hyprland/window"],
    "modules-center": ["mpd", "custom/media"],
    "modules-right": [
      "cpu",
      "memory",
      "pulseaudio",
      "battery",
      "network",
      "clock",
      "tray"
    ]
  },

  {
    // Bar settings
    "layer": "top",
    "position": "top",
    "output": ["DP-1"],

    "include": [
      "~/dotfiles/waybar/modules.json",
      "~/dotfiles/waybar/modules-nolimit-length.json"
    ],

    // Modules
    "modules-left": ["wlr/workspaces", "hyprland/submap", "hyprland/window"],
    "modules-center": ["mpd", "custom/media"],
    "modules-right": [
      "cpu",
      "memory",
      "pulseaudio",
      "battery",
      "network",
      "clock",
      "tray"
    ]
  }
]
